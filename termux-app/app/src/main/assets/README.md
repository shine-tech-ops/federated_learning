# 前端需要解决的任务

## 一、修复并优化CNN模型任务训练问题

演示的时候无法正常执行训练任务，需要检查并修复问题

进度：已完成

## 二、多台设备训练，确保模型精度

1. 需要可用的多台设备（需要再邮寄一些样机）
2. 优化训练代码，只保留最高精准度的哪一个
3. 验证模型精度

## 三、客户端界面

### 前端界面
iOS、Android

现在在 `Android` 中渲染 `HTML` 实现

不再使用模型选择，而且选择功能：训练、对话

对话功能：选择大模型、模型对话（云端对话）

模型对话都保留上传对话记录按钮

### 训练模块
更多示例代码

接收训练任务（通过MQTT）

执行训练任务

### 对话模块
#### 小模型（本地训练的模型）
1. 保存对话历史，上传到服务端
2. 更新新的模型（下载新模型进行替换，或保留原模型，仅切换到新模型）

（重要1）

**从服务器端下载模型，使用ollama添加运行（按钮一键下载、部署、运行）**

#### 大模型（云端模型）
1. 对接大模型API
2. 保存对话历史，上传到服务端

## 四、解决iOS训练环境问题
可行的策略：iSH、UTM、a-shell

iSH 是一个用于运行 Linux 发行版的实用程序。我不认为它像 Termux 那样具备与操作系统交互的能力，或许在越狱设备上它会更强大一些。但由于它是一个模拟器，某些功能（例如 PulseAudio）可能无法使用。

UTM 需要越狱

UTM SE 不需要越狱
